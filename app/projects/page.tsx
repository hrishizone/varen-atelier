import Image from 'next/image';import { projects } from '@/lib/data';
export default function Projects(){return <section className="section pt-36"><div className="mx-auto max-w-7xl"><h1 className="serif text-8xl">Projects</h1>{projects.map(p=><article key={p.slug} className="my-12 grid gap-6 md:grid-cols-[40%_1fr]"><div><p className="uppercase tracking-[.25em] text-walnut">{p.type} · {p.location}</p><h2 className="serif mt-3 text-5xl">{p.name}</h2><p className="mt-5 text-charcoal/65">{p.summary}</p></div><div className="relative h-[65vh] overflow-hidden rounded-[32px]"><Image fill src={p.image} alt={p.name} className="object-cover"/></div></article>)}</div></section>}
