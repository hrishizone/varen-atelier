import Image from 'next/image';import Link from 'next/link';import { collections } from '@/lib/data';
export default function Collections(){return <section className="section pt-36"><div className="mx-auto max-w-7xl"><h1 className="serif text-7xl md:text-9xl">Collections</h1><div className="mt-12 grid gap-8">{collections.map(c=><Link className="group grid gap-6 border-t border-charcoal/15 py-8 md:grid-cols-2" href={`/collections/${c.slug}`} key={c.slug}><div><p className="uppercase tracking-[.3em] text-walnut">{c.kicker}</p><h2 className="serif mt-3 text-6xl">{c.name}</h2><p className="mt-5 max-w-xl text-charcoal/65">{c.description}</p></div><div className="relative h-96 overflow-hidden rounded-[30px]"><Image fill src={c.image} alt={c.name} className="object-cover transition group-hover:scale-105"/></div></Link>)}</div></div></section>}
