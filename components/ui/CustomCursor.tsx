'use client';import { motion,useMotionValue,useSpring } from 'framer-motion';import { useEffect } from 'react';
export default function CustomCursor(){const x=useMotionValue(-100),y=useMotionValue(-100);const sx=useSpring(x,{stiffness:180,damping:24}),sy=useSpring(y,{stiffness:180,damping:24});useEffect(()=>{const move=(e:MouseEvent)=>{x.set(e.clientX-16);y.set(e.clientY-16)};window.addEventListener('mousemove',move);return()=>window.removeEventListener('mousemove',move)},[x,y]);return <motion.div aria-hidden className="pointer-events-none fixed z-[80] hidden h-8 w-8 rounded-full border border-brass mix-blend-difference md:block" style={{x:sx,y:sy}}/>}
